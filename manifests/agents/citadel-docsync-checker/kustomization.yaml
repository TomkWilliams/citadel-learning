apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: citadel-learning

commonLabels:
  app.kubernetes.io/name: citadel-docsync-checker
  app.kubernetes.io/part-of: citadel-agents
  operations.tomkw.dev/agent-level: "0"
  operations.tomkw.dev/hosting-zone: citadel-learning

# Review-only skeleton. Do not enable resources until the image, context bundle,
# outbox storage, and model endpoint have been reviewed.
resources: []

# Planned resources:
# - serviceaccount.yaml
# - configmap-context-profile.yaml
# - networkpolicy.yaml
# - job.yaml
