apiVersion: v1
kind: ServiceAccount
metadata:
  name: citadel-docsync-checker
  namespace: citadel-learning
  labels:
    app.kubernetes.io/name: citadel-docsync-checker
    app.kubernetes.io/part-of: citadel-agents
automountServiceAccountToken: false
# No Role or RoleBinding by design: rbac_scope is "none" per the agent registry
# and design record. Do not add one without a new design review.
